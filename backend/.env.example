# ====================================
# BACKEND ENVIRONMENT VARIABLES
# ====================================
# Copy this file to .env and fill in your values
# NEVER commit .env to git - use .env.example as template only

# === Core Configuration ===
NODE_ENV=production
PORT=5000

# === Database ===
# MongoDB Connection String
# Local: mongodb://admin:password@localhost:27017/rent-management?authSource=admin
# Atlas: mongodb+srv://username:password@cluster.mongodb.net/rent-management
MONGODB_URI=mongodb+srv://your_username:your_password@your_cluster.mongodb.net/rent-management

# === Authentication ===
# Generate strong JWT_SECRET: use openssl rand -hex 32 or similar
# Must be at least 32 characters
JWT_SECRET=your_super_secret_jwt_key_here_change_this_in_production

# === CORS & API ===
# Your frontend domain (include https://)
CORS_ORIGIN=https://yourdomain.com

# === Logging ===
# Levels: debug, info, warn, error
LOG_LEVEL=info

# === Session Management (Optional) ===
SESSION_SECRET=your_session_secret_here_change_this

# === Email Service (Optional - for notifications) ===
# SMTP configuration for sending emails
# EMAIL_FROM=noreply@yourdomain.com
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your_email@gmail.com
# EMAIL_PASSWORD=your_app_specific_password

# === Payment Gateway (Optional - for future integration) ===
# RAZORPAY_KEY_ID=your_key
# RAZORPAY_KEY_SECRET=your_secret
# STRIPE_PUBLIC_KEY=your_key
# STRIPE_SECRET_KEY=your_secret

# === Security (Optional) ===
# Rate limiting - requests per minute
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# === File Upload (Optional) ===
# MAX_UPLOAD_SIZE=5242880  # 5MB in bytes

# === Analytics (Optional) ===
# SENTRY_DSN=your_sentry_dsn
